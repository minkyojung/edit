dist/
node_modules/
src-tauri/target/
src-tauri/binaries/
sidecar-pkg/

# Google OAuth credentials — baked in at compile time via include_str!, kept
# out of git (see src-tauri/secrets/README.md). Values only, never the README.
src-tauri/secrets/google_client_id
src-tauri/secrets/google_client_secret
