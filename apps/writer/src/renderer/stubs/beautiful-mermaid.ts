// Stub for beautiful-mermaid — mermaid diagram rendering is not needed in Zurich.
export const parseMermaid = (_code: string): null => null
export const renderMermaidSVG = (_code: string): string => ''
