import React from 'react'

export default function App(): React.ReactElement {
  return (
    <div
      style={{
        height: '100vh',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        fontFamily: 'system-ui, sans-serif',
        color: '#1a1a1a',
        background: '#fafafa'
      }}
    >
      <p style={{ fontSize: 18, opacity: 0.5 }}>Writer — P1 세팅 완료</p>
    </div>
  )
}
