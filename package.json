{
  "name": "montpellier",
  "private": true,
  "scripts": {
    "setup": "pnpm install && pnpm rebuild electron",
    "dev": "pnpm --filter writer dev",
    "build": "pnpm --filter writer build"
  },
  "dependencies": {
    "proof-sdk": "github:EveryInc/proof-sdk",
    "tsx": "^4.21.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["electron", "esbuild", "better-sqlite3", "@resvg/resvg-js"]
  }
}
