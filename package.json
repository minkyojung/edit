{
  "name": "montpellier",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter writer-tauri tauri dev",
    "build": "pnpm --filter writer-tauri tauri build"
  },
  "dependencies": {
    "proof-sdk": "link:../proof-sdk"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.1"
  }
}
